{
  "name": "Full Transparency",
  "description": "Baseline scenario with all data fields at their default (enabled) values, verifying the AI correctly synthesizes the full set of pricing, scoring, demand, seller, and content information",
  "config": {}
}
