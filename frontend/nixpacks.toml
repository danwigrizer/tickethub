# Frontend service - use Dockerfile
# This prevents Railway from auto-detecting Next.js build

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

